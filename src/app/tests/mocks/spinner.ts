import { Component } from '@angular/core';

@Component({
  selector: 'mat-progress-spinner', // tslint:disable-line
  template: '[mat-progress-spinner]',
})
export class MatProgressSpinnerComponent {}
